Документация проекта

## Описание базового синтаксиса записи и форматирования

Проект использует следующие технологии и синтаксис:

1. **Python**: Основной язык программирования для написания тестов.
2. **Selenium**: Библиотека для автоматизации взаимодействия с веб-браузером.
3. **Pytest**: Фреймворк для написания и запуска тестов.
4. **Allure**: Инструмент для генерации отчетов о выполнении тестов.

 Форматирование кода

- Код форматируется в соответствии с PEP 8 (стиль написания кода на Python).
- Используются docstrings для документирования методов и функций.
- Все шаги теста размечаются с помощью `@allure.step` или `with allure.step` для улучшения читаемости отчетов.

---
 Инструкция по запуску тестов для формирования отчета Allure

1. Установите необходимые зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Запустите тесты с генерацией отчета Allure:
   ```bash
pytest . --alluredir=./allure-results
   ```

   Эта команда запустит все тесты и сохранит результаты в директорию `./allure-results`.

---

Инструкция по просмотру сформированного отчета Allure

1. После завершения тестов, чтобы просмотреть отчет, выполните команду:
   ```bash
   allure serve ./allure-results
   ```

   Эта команда запустит локальный сервер и откроет отчет в браузере.

2. В отчете Allure вы увидите три теста:
   
   1. "Проверка формы заполнения"
   -   Этот тест проверяет подсветку полей разными цветами после отправки.
   - Шаги теста:
     - Запуск браузера Microsoft Edge
     - Заполнение полей:
           Заполнение полей ввода 'Иван', 'Петров', 'Ленина, 55-3', '', # 'Москва', 'Россия', 'test@skypro.com', '+7985899998787', 'QA', 'SkyPro'
     - Нажатие кнопки отправки формы
     - Получение CSS-класса поля Zip-code45ms
     - Проверка, что это поле подсвечено красным цветом
     - Проверка, что все остальные поля подсвечены зеленым цветом
     - Закрытие браузера
   
   - Результат: Успешное выполнение.

    2. "Проверка работоспособности калькулятора"
   - Этот тест позволяет проверить правильность вычислений на калькуляторе с задержкой
   - Шаги теста:
   - Запуск браузера Chrome
   - Открытие страницы калькулятора
   - Настройка задержки:
         Установка задержки на 45 секунд
             time	45
   - Нажатие кнопок:
         Нажатие кнопок: ('7', '+', '8', '=')
             buttons	('7', '+', '8', '=')
   - Ожидание и получение результата:
         Ожидание и получение результата: «15» 
             expected_value	'15'
   - Сравнение результата с ожидаемым
   - Закрытие браузера

   - Результат: Успешное выполнение.

    3. "Проверка основных функций интернет-магазина"
   Этот тест проверztn авторизацию, добавление товаров в корзину и итоговую сумму покупки.
   - Шаги теста:
     - Подготовка и вход:
           Запуск браузера Firefox
           Ввод логина и пароля пользователя
           Заполнение полей ввода 'standard_user', 'secret_sauce' 
               name	'standard_user'
               password	'secret_sauce'
           Нажатие кнопки Login для входа в систему
     - Выбор товаров:
           Добавление товаров в корзину по их ID
               Находит товары по ID и кликает по ним
           Клик по иконке корзины
     - Нажатие кнопки Checkout
     - Ввод данных и проверка суммы:
           Заполняет форму:
           Заполнение полей ввода 'Анастасия', 'Береза', '123456' 3 parameters461ms
               first_name	'Анастасия'
               last_name	'Береза'
                postal_code	'123456'
     - Ожидание появления суммы:
           окончательная проверка итоговой суммы заказа
           Сравнение полученной суммы с ожидаемой
     - Закрытие браузера
     
   - Результат: Успешное выполнение.